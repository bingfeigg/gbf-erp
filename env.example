# Runtime
NODE_ENV=production
PORT=3100

# Security (REQUIRED in production)
JWT_SECRET=replace-with-a-strong-random-secret-min-32-chars

# Optional scanner defaults (can be overridden by alert_rules table)
APPROVAL_OVERDUE_HOURS=24
APPROVAL_SCAN_INTERVAL_MS=300000
